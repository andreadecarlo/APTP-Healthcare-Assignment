(define (problem problem3) (:domain healthcare)
    (:objects 
        r1 - robot-box

        u1 u2 u3 - unit
        l1 l2 l3 - location
    )

    (:htn
        :parameters ()
        :subtasks (and
            (task0 (get_to r1 l3))
        )
    )
    (:init
        (at r1 central_warehouse)

        (connected central_warehouse l1)
        (connected l1 l2)
        (connected l2 l3)
    )

)